
// Job paramaters to set for this environment

params.scratch = true

gatk_bundle_path = "/work_ifs/sukmb352/references/gatk/v2"

// Resource manager settings

executor {
  queueSize=100
}

// Process setting

process {
  
  executor = 'slurm'
  queue = 'htc'
 
}

params.max_cpus = 20
params.max_ram = 240

singularity {
 enabled = true
 runOptions = "-B /work_ifs"
}

params {
  // Defaults only, expecting to be overwritten
  max_memory = 250.GB
  max_cpus = 20
  max_time = 240.h
  maxMultiqcEmailFileSize = 25.MB
}


