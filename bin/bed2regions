#!/bin/env ruby

f = File.open(ARGV.shift, "r")
chr = ARGV.shift

while (line = f.gets)
	next if line.match(/^#.*/)
	
	seq,from,to,name = line.strip.split("\t")

	next unless seq == chr

        puts "#{seq}:#{from}-#{to}"

end

