#!/bin/env ruby

chr = ARGV.shift
lines = $stdin

lines.each do |line|
	next if line.match(/^#.*/) or line.match(/^@.*/)
	
	seq,from,to,name = line.strip.split("\t")

	next unless seq == chr

        puts "#{seq}:#{from}-#{to}"
end

